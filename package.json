{
  "name": "susalert-updated",
  "version": "1.3.1",
  "private": true,
  "description": "Standalone read-only Alt1 app for Croesus alerts, a movable next-special countdown, and party route tracking.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "node tools/verify-update.js",
    "check": "node tools/verify-update.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
